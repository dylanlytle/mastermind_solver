import 'package:flutter/material.dart';

var lightColorScheme = ColorScheme.fromSeed(seedColor: Colors.deepPurple);
var darkColorScheme = ColorScheme.fromSeed(
    seedColor: Colors.deepPurple, brightness: Brightness.dark);
